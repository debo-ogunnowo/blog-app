<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        header {
            background: #333;
            color: #fff;
            padding: 10px 20px;
            position: absolute;
            top: 0;
            width: 100%;
            text-align: center;
        }
        .container {
            text-align: center;
        }
        .no-posts-message {
            font-size: 2rem;
            color: #555;
            margin-bottom: 20px;
        }
        .create-post button {
            background: #17a2b8;
            color: #fff;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
        }
        .create-post button:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>
    <header>
        <h1>MyBlog</h1>
    </header>
    <div class="container">
        <!-- Message when no posts are available -->
        <div class="no-posts-message">No posts added yet</div>
        <div class="create-post">
            <button onclick="window.location.href='/api/posts/new'">Create posts</button>
        </div>
    </div>
</body>
</html>
